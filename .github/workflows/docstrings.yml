name: Docstring Coverage Check

on:
  pull_request:
    branches:
      - main

jobs:
  docstrings:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup
        uses: './.github/actions/cicd-setup'
      - name: Check docstring coverage
        run: interrogate freegsnke/
